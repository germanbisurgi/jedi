{
  "title": "`x-messages`",
  "description": "Validation error messages can be customized using the `x-messages` option in the schema. When defined as an object, messages can be applied per language (e.g., `en`, `fr`) and constraint (e.g., `minLength`, `const`).",
  "type": "object",
  "properties": {
    "string": {
      "type": "string",
      "minLength": 5,
      "const": "locoloco",
      "x-messages": {
        "en": {
          "minLength": "Need at least 5 sparks of brilliance.",
          "const": "Only 'locoloco' unlocks the magic here."
        }
      }
    }
  }
}
