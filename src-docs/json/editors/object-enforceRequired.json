{
  "title": "enforceRequired",
  "description": "Editor should display all required properties when the option `enforceRequired` or the schema `x-` option `enforceRequired` is true",
  "type": "object",
  "default": {
    "a": 1
  },
  "required": [
    "a",
    "b"
  ],
  "properties": {
    "a": {
      "type": "integer"
    },
    "b": {
      "type": "integer"
    }
  }
}