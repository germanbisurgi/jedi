{
  "title": "enforceRequired",
  "description": "Editor should display all required properties when the jedi option `enforceRequired` or the schema `x-enforceRequired` is true",
  "type": "object",
  "default": {
    "a": 1
  },
  "required": [
    "a",
    "b"
  ],
  "properties": {
    "a": {
      "type": "integer"
    },
    "b": {
      "type": "integer"
    }
  }
}