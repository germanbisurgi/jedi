{
  "title": "minimum",
  "type": "object",
  "description": "Ranges of numbers are specified using a combination of the minimum and maximum keywords, (or exclusiveMinimum and exclusiveMaximum for expressing exclusive range).",
  "properties": {
    "number": {
      "type": "number",
      "minimum": 100
    },
    "integer": {
      "type": "integer",
      "minimum": 100
    }
  }
}
